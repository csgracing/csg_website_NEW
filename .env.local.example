# Copy to .env.local and fill in when Instagram integration is implemented.
INSTAGRAM_ACCESS_TOKEN=

# Resend API key (resend.com), powers the Contact Us "Apply Now" button and
# Get In Touch form, sending to team@csg.racing server-side. Also verify a
# sending domain in Resend and swap the "from" address in
# src/app/api/contact/route.ts away from the onboarding@resend.dev sandbox.
RESEND_API_KEY=
